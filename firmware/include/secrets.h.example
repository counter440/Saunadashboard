#pragma once
// Copy this file to `firmware/include/secrets.h` and fill in real values.
// The real file is gitignored.

// Device identity (matches what you provisioned in /admin/devices/new)
#define DEVICE_ID            "sauna-dev-01"

// ─── Wi-Fi (Phase A/B) ────────────────────────────────────────────────
#define WIFI_SSID            "your-ssid"
#define WIFI_PASSWORD        "your-wifi-password"

// ─── MQTT broker ──────────────────────────────────────────────────────
// Phase A: your bench machine's LAN IP, port 1883 (plaintext)
// Phase D: production hostname, port 8883 (TLS)
#define MQTT_HOST            "192.168.1.10"
#define MQTT_PORT            1883
#define MQTT_USERNAME        "sauna-dev-01"
#define MQTT_PASSWORD        "the-password-from-admin-devices-new"

// ─── LTE-M / cellular (Phase C+) ──────────────────────────────────────
#define CELLULAR_APN         "telenor.iot"
#define CELLULAR_USER        ""
#define CELLULAR_PASS        ""
